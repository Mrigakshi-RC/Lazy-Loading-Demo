import React from "react";

const OrderFood = () => {
  return (
    <div style={{ fontSize: "2rem", width: "500px", margin: "auto" }}>
      Order Food: a component with a lot of child components inside
    </div>
  );
};

export default OrderFood;
